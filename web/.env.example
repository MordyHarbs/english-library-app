# Copy to .env.local and fill in.
# LOCAL values come from running `supabase start` (see the "API URL" and "anon key").
# PROD values come from your Supabase project's API settings, set in Netlify env vars.

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-local-anon-key-here
